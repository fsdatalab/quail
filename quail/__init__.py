"""Quail: a QUery-Aware Inference Layer for AI filter queries.

A custom scheduler and memory manager over vLLM. The query plan, not
the engine's heuristics, decides what enters memory and what stays.
"""
