"""Stock vLLM baselines - dev dependency only, never imported by the
engine. Per house rule, the baseline gets every plan-derived setting
in its analytically equivalent form, and the submission strategy is
named in every result."""
